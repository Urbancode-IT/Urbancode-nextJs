import blogsData from '../../../lib/data/blogsData.json';

// We export the JSON data to satisfy the requirement of having a blogsData.js file
// and to allow components to import it directly.
export const recentBlogs = blogsData;
export default blogsData;
