export default function EnrollNowLayout({ children }) {
  return (
    <div style={{ minHeight: "100vh", background: "linear-gradient(135deg, #0a3d1f 0%, #036c2d 50%, #1a8a3d 100%)" }}>
      {children}
    </div>
  );
}
